@inherits UiComponentBase

@if (!string.IsNullOrEmpty(href))
{
    <a href=@href class=@ClassNames(BaseClass, SecondaryClass, Class) title=@title target=@target @onclick:stopPropagation @attributes="IncludeAttributes">
        @ChildContent
    </a>
}
else
{
    <button type=@type @onclick="onclick" class=@ClassNames(BaseClass, SecondaryClass, Class) @attributes="IncludeAttributes">
        @ChildContent
    </button>
}