@page "/gallery/autoform/create"

<AutoCreateForm Model="Booking" ApiType="typeof(CreateBooking)" Save="Save" />

@{
    // Save method
    void Save(Booking booking)
    {
        // Save booking
        Console.WriteLine(booking);
    }
}
