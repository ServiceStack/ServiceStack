name: Build Text

on:
  push:
    paths:
      - 'ServiceStack.Text/**'
      - '.github/workflows/build-text.yml'

jobs:
  build-text:
    uses: ServiceStack/ServiceStack/.github/workflows/reuse-build-text.yml@main
    secrets:
      SERVICESTACK_LICENSE: ${{ secrets.SERVICESTACK_LICENSE }}
          